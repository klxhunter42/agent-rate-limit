{{- if .Values.rlDashboard.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ai-gateway.component" (dict "Release" .Release "Chart" .Chart "component" "rl-dashboard") }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "ai-gateway.labels" (dict "Chart" .Chart "Release" .Release "component" "rl-dashboard") | nindent 4 }}
spec:
  replicas: {{ .Values.rlDashboard.replicaCount }}
  selector:
    matchLabels:
      {{- include "ai-gateway.selectorLabels" (dict "Chart" .Chart "Release" .Release "component" "rl-dashboard") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ai-gateway.selectorLabels" (dict "Chart" .Chart "Release" .Release "component" "rl-dashboard") | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets: {{ toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- include "ai-gateway.podSecurityContext" . | nindent 8 }}
      containers:
        - name: rl-dashboard
          image: "{{ .Values.rlDashboard.image.repository }}:{{ .Values.rlDashboard.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.rlDashboard.image.pullPolicy }}
          ports:
            - containerPort: 8080
              name: http
          securityContext:
            {{- include "ai-gateway.securityContext" . | nindent 12 }}
          resources:
            {{- toYaml .Values.rlDashboard.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
{{- end }}
