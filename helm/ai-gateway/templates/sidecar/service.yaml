{{- if .Values.sidecar.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-gateway.component" (dict "Release" .Release "Chart" .Chart "component" "sidecar") }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "ai-gateway.labels" (dict "Chart" .Chart "Release" .Release "component" "sidecar") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.sidecar.service.port }}
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    {{- include "ai-gateway.selectorLabels" (dict "Chart" .Chart "Release" .Release "component" "sidecar") | nindent 4 }}
{{- end }}
