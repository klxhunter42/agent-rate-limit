# =============================================================================
# Multi-Agent AI System - Environment Configuration
# Copy this to .env and fill in your API keys
#
# IMPORTANT: If you don't use a provider, either leave the value empty
# or remove the line entirely. The system will skip providers without keys
# =============================================================================

# --- API Gateway ---
GATEWAY_PORT=8080
GLOBAL_RATE_LIMIT=100
AGENT_RATE_LIMIT=5
WORKER_POOL_SIZE=100
UPSTREAM_URL=https://api.z.ai/api/anthropic
STREAM_TIMEOUT=300s
MAX_REQUEST_BODY=10485760

# --- GLM Mode Toggle ---
# GLM_MODE=true: Z.AI features active (vision, key pool, model limits, GLM defaults, zai fallback)
# GLM_MODE=false: pure multi-provider proxy (Claude/Gemini/OpenAI/etc)
GLM_MODE=false
# GLM/zai-only tuning (do not affect claude-oauth):
# STRIP_GLM_TOOL_XML=false         # strip XML tool blocks from glm streams; OFF by default (buffering freezes SSE on '<')
# TEXTCOMP_MAX_BODY_BYTES=65536    # skip TextComp (token compression) for bodies larger than this (speed-first)
# OVERLOAD_MODEL_FALLBACK=true     # on z.ai 529, switch to a same-series sibling model using the same BYOK key
# ZAI_THINKING_BUDGET=0            # 0 = strip thinking config (kills 20-75s extended thinking, speed-first); >0 = cap budget_tokens

# --- Default Request Values ---
# DEFAULT_MODEL=glm-5.2
# DEFAULT_PROVIDER=glm
# DEFAULT_TEMPERATURE=0.7
# DEFAULT_MAX_TOKENS=1024

# --- Z.AI (Primary Provider) ---
# ZAI_API_KEYS=key1,key2

# --- Anthropic ---
# ANTHROPIC_API_KEYS=
# ANTHROPIC_UPSTREAM_BASE=https://api.anthropic.com
# ANTHROPIC_API_VERSION=2023-06-01

# --- Claude OAuth ---
# CLAUDE_OAUTH_CLIENT_ID=
# CLAUDE_UPSTREAM_BASE=https://claude.ai

# --- Google Gemini ---
# GEMINI_OAUTH_CLIENT_ID=
# GEMINI_OAUTH_CLIENT_SECRET=
# GEMINI_UPSTREAM_BASE=https://generativelanguage.googleapis.com
# GEMINI_API_ENDPOINT=https://generativelanguage.googleapis.com
# GEMINI_CODEASSIST_BASE=https://cloudcode-pa.googleapis.com/v1internal
# GEMINI_CODEASSIST_ENDPOINT=https://cloudcode-pa.googleapis.com/v1internal
# GEMINI_DEFAULT_MODEL=models/gemini-2.5-flash-preview-05-20

# --- OpenAI ---
# OPENAI_UPSTREAM_BASE=https://api.openai.com

# --- OpenRouter ---
# OPENROUTER_UPSTREAM_BASE=https://openrouter.ai/api

# --- DeepSeek ---
# DEEPSEEK_UPSTREAM_BASE=https://api.deepseek.com

# --- Kimi (Moonshot) ---
# KIMI_UPSTREAM_BASE=https://api.moonshot.cn/v1

# --- HuggingFace ---
# HUGGINGFACE_UPSTREAM_BASE=https://api-inference.huggingface.co/models

# --- Ollama ---
# OLLAMA_UPSTREAM_BASE=http://localhost:11434

# --- AGY (Antigravity) ---
# AGY_UPSTREAM_BASE=https://antigravity.com

# --- Cursor ---
# CURSOR_UPSTREAM_BASE=https://api2.cursor.sh

# --- CodeBuddy ---
# CODEBUDDY_UPSTREAM_BASE=https://api.codebuddy.io

# --- Kilo ---
# KILO_UPSTREAM_BASE=https://api.kilo.ai

# --- Lotus LLM ---
# LOTUS_UPSTREAM_BASE=https://llm.internal/custom/llm

# --- Qwen ---
# QWEN_UPSTREAM_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1

# --- Adaptive Limiter ---
# MODEL_PRIORITY=glm-5.2:100,glm-5.1:95,glm-5-turbo:90,glm-5:80,glm-4.7:70,glm-4.6:60,glm-4.5:50

# --- Dashboard Auth ---
DASHBOARD_PASSWORD=changeme

# --- PasteGuard Privacy Masking ---
PASTEGUARD_ENABLED=true
PASTEGUARD_SECRETS_ENABLED=true
# PASTEGUARD_SECRET_ENTITIES=OPENSSH_PRIVATE_KEY,PEM_PRIVATE_KEY,API_KEY_SK,API_KEY_AWS,API_KEY_GITHUB,API_KEY_GITLAB,JWT_TOKEN,BEARER_TOKEN,ENV_PASSWORD,ENV_SECRET,CONNECTION_STRING,API_KEY_GCP,API_KEY_TENCENT,API_KEY_ALIBABA,API_KEY_SLACK,API_KEY_STRIPE,API_KEY_SENDGRID,ENV_TOKEN,ENV_CREDENTIAL,BASIC_AUTH_URL,VAULT_TOKEN,AZURE_CREDENTIAL
PASTEGUARD_MAX_SCAN_CHARS=200000
PASTEGUARD_PII_ENABLED=true
# PASTEGUARD_PII_ENTITIES=EMAIL_ADDRESS,PHONE_NUMBER,CREDIT_CARD,SSN,IBAN,IP_ADDRESS,THAI_NATIONAL_ID,THAI_PHONE

# --- Docker Platform ---
# linux/arm64 for Apple Silicon / ARM servers
# linux/amd64 for Intel / AMD servers
DOCKER_PLATFORM=linux/arm64

# --- Observability ---
GRAFANA_PORT=3000
GRAFANA_ADMIN_PASSWORD=changeme

# --- Dashboard UI ---
VITE_ALLOWED_HOSTS=localhost
# VITE_HMR_HOST=
# VITE_HMR_PORT=

# --- Central Proxy ---
PROXY_PORT=9000
# PROXY_HOST=
PROXY_SCHEME=http

# --- PostgreSQL (durable storage tier; opt-in via --profile storage) ---
# Start: docker compose --profile storage up arl-postgres arl-gateway
# Override POSTGRES_HOST to use an external Postgres instead of the bundled one.
POSTGRES_HOST=arl-postgres
POSTGRES_PORT=5432


POSTGRES_DB=arl
POSTGRES_SSLMODE=disable

# --- Traffic Capture (upstream req/resp -> S3-compatible object store / Tencent COS) ---
# Persists the exact request the gateway sends upstream and the raw provider
# response. Async; never blocks the request path. Auth headers are redacted.
TRAFFIC_CAPTURE_ENABLED=false
COS_ENDPOINT=          # e.g. cos.ap-bangkok.myqcloud.com (no scheme)
COS_REGION=            # e.g. ap-bangkok
COS_BUCKET=            # e.g. ai-gateway-traffic-1300000000
COS_ACCESS_KEY_ID=     # COS SecretId
COS_SECRET_ACCESS_KEY= # COS SecretKey
# COS_PREFIX=traffic
# COS_INSECURE=false           # true only for non-TLS endpoints (testing)
# TRAFFIC_CAPTURE_MAX_BODY_BYTES=5242880   # cap per req/resp body (5MB)
# TRAFFIC_CAPTURE_QUEUE_SIZE=1024          # records dropped when full
# TRAFFIC_CAPTURE_WORKERS=4
